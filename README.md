### STEP 01 - Create a conda environment after opening the repository & install the requirements

```bash
conda create -n llmap python=3.10 -y
```

```bash
conda activate llmap
pip install -r requirements.txt
```
